context("Report building")

test_that("report builds with each option and in combination", {
# how will i test this.
})

# build_report(AG195, "~/Desktop/",
#              proj4 = "+proj=utm +zone=33 +south +datum=WGS84 +units=m +no_defs",
#              stats = c("rolling", "diurnal", "lunar", "seasonal"),
#              seas = c("2010-02-15", "2010-07-15", "2010-11-15"),
#              wavelet = c("dist", "rel.angle"),
#              construct = c("klocoh", "akde"))
